import './Contact.scss'

const Contact = ({ id }) => {
    return (
        <section className="c-contact">
          <div className="contact-container" id={id}>

          </div>
        </section>
    );
}

export default Contact;